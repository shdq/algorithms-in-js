const getProduct = require("./problem-27");

describe("Tests for problem 27: Quadratic primes", () => {
  test("empty function returns undefined", () => {
    expect(getProduct()).toBe(undefined);
  });
});
