const { product } = require("./problem-32");

/* eslint max-len: 0 */
describe("Tests for problem 32: Pandigital products", () => {
  test("initial test", () => {
    expect(product()).toBe(undefined);
  });
});
